import { ButtonVariant } from "@/shared/components/Button/VButton.types";
import { ColorMode } from "@/shared/types";

export type ButtonStyle = {
  label: string;
  variant: ButtonVariant;
  color: ColorMode;
};
